{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center align-items-center">
            <h1>New Posts</h1>
        </div>
    {% for page in pages %}
        <div class="row"><a href="page/{{ page.page_name }}">{{ page.title }}</a></div>
    {% endfor %}
    </div>
{% endblock %}
